# AGENTS.md — shotpvp 项目说明

> 本文件面向后续接手本项目的 AI 助手与开发者，记录项目现状、架构约定、可扩展方向与优化点。

## 一、项目现状

终端双人坦克对战原型。一个权威服务端进程 + 两个客户端进程，通过 UDP 通信，本地 `127.0.0.1:47000` 跑通。

### 文件结构

| 文件 | 职责 |
|---|---|
| `protocol.py` | UDP v3 二进制协议与完整世界快照编解码、严格字段校验 |
| `game.py` | 纯游戏逻辑与权威 `World`：地图、对局、坦克、反弹子弹、道具、重生 |
| `input_win.py` | Windows Console API 真实按键事件采集（ctypes + ReadConsoleInputW） |
| `renderer.py` | ANSI 差分渲染：只重绘变化的格子，避免整屏闪烁 |
| `server.py` | 权威服务端：接收线程 + 30Hz 主循环，推进世界并广播 STATE |
| `client.py` | 客户端：输入采集 → 发 INPUT → 收 STATE → 本地预测 → 渲染 |
| `connection_code.py` | 版本化、带 CRC、10 分钟过期的直连 offer/answer 编解码 |
| `connectivity.py` | LAN/IPv6/STUN/UPnP 候选、HMAC 探测、重放保护、RTT 选路与 keepalive |
| `test_logic.py` | 游戏规则与对局状态回归测试 |
| `test_protocol.py` | 协议 v3 往返与坏包校验测试 |
| `test_renderer.py` | 多格坦克、墙体、子弹和道具的纯 buffer 测试 |
| `test_input.py` | 按键读取回显测试 |
| `README.md` | 启动方式 |

### 核心参数

- 逻辑帧：30Hz（`TICK_DT = 1/30`）
- 渲染帧：60Hz（独立于逻辑帧）
- 地图：64×30 格，0=空、1=砖墙（可破坏）、2=钢墙
- 对局：3 秒倒计时，先到 5 分获胜，双方开火确认重开
- 道具：每 8 秒尝试生成，场上最多 2 个（缩小、三连发、一次性护盾）
- 坦克：开火冷却 8 tick（≈0.27s），被击杀后 15 tick（≈0.5s）重生
- 子弹：每 tick 推进 1 格，撞边界/墙反弹，砖墙同时破坏，最多反弹 5 次，可自伤
- 操作：方向键四向移动；WASD 支持 WA/WD/SA/SD 八方向组合；空格/回车开火，ESC 退出

### 同步模型

**状态同步**（非帧同步）：
- 服务端权威，30Hz 广播完整世界快照（地图、阶段、坦克、子弹、道具）
- 客户端按键立即发 INPUT 包，同时对**本地坦克**做预测移动（不等回包）
- 服务端 STATE 回来后，用服务器位置覆盖本地坦克（纠正预测漂移）
- 远端坦克使用约 100ms 快照缓冲插值

### 已知限制

- 仅 Windows（输入层依赖 Win32 Console API）
- 无重连；客户端可提示状态中断，但服务端尚无完整掉线回收
- 地图固定，无随机生成
- 无启动菜单 / 匹配界面

## 二、架构约定

### 分层原则

- `game.py` 是**纯逻辑层**，不依赖网络、不依赖终端、不依赖第三方库。任何端（服务端/客户端/未来观战端/测试）都直接 import 它。新增游戏规则优先改这里。
- `protocol.py` 是**协议层**，只管字节编解码，不含业务语义。改协议只改这里，两端自动同步。
- `input_win.py` / `renderer.py` 是**平台层**，可按平台替换（未来 Linux 版写 `input_linux.py`，客户端按平台 import）。
- `server.py` / `client.py` 是**应用层**，组合以上三层。

### 坐标与状态

- 所有坐标是**整数格**，无浮点。这是状态同步友好（快照小、确定性无关）也是终端渲染友好（字符网格天然离散）。
- 方向用 `0..3=上/下/左/右`、`4..7=左上/右上/左下/右下`，`DIR_DELTA` 提供整数位移。
- Tank/Bullet 用 `__slots__`，序列化走 `to_tuple()` → struct pack。

### 渲染

- 差分渲染：`Renderer` 维护上一帧 buffer，只输出变化的格子（ANSI 光标定位 + 重写）。
- 颜色用 ANSI 前景色码，玩家蓝、对手红、子弹黄、墙灰。
- HUD 画在地图下方一行。

## 三、可扩展方向

### A. 网络：从本地到远程

当前已有实验性无服务器直连：主机客户端嵌入 `GameServer`，双方通过临时连接码交换候选，
认证探测后选最低 RTT 路径。STUN/UPnP 失败会降级，但对称 NAT 仍需未来 TURN/VPS 兜底。

1. **VPS 中转服务器**（推荐起点）
   - 把 `server.py` 部署到国内 VPS，客户端 `SERVER_HOST` 改为 VPS IP。
   - 延迟 = 双方到 VPS 的 RTT 之和，国内节点可压到 30-60ms。
   - 改动量极小，是远程联机的最小可行路径。

2. **NAT 穿透 / P2P**
   - WebRTC DataChannel（Python 用 `aiortc`）：直连延迟最低，但打洞不一定成功，需 TURN 兜底。
   - 自建 UDP 打洞：手写 STUN 协议获取公网映射地址，双方互发。可控但费事。
   - 坦克大战实体少、带宽低，P2P 收益有限，建议先中转跑通再考虑。

3. **房间与匹配**
   - 服务端支持多房间：`room_id` 字段加入 JOIN，服务端按 room 分组管理多组对战。
   - 简单匹配：服务端维护等待队列，凑满 2 人分配同一 room。

### B. 同步优化（远程联机的关键）

4. **远端插值**（最重要）
   - 客户端保留最近两个 STATE 快照 `prev` / `curr`，在两帧之间按 tick 进度线性插值远端坦克位置。
   - 视觉延迟 = 插值缓冲（建议 100ms），换来平滑感。坦克大战对延迟容忍度高，这点延迟完全可接受。
   - `client.py` 已预留 `prev_snapshot` / `curr_snapshot`，插值逻辑待填。

5. **客户端预测改进**
   - 当前预测只推进一格、不检查与远端坦克碰撞。改进：预测时也做本地坦克与远端坦克的碰撞预判，减少穿模。
   - 预测冲突纠正：服务器位置与预测偏差超阈值时用插值过渡而非硬拉（当前是硬覆盖）。

6. **可靠包与丢包补偿**
   - INPUT 包当前是纯 UDP，丢包即丢输入。关键操作（开火）可走可靠通道：给 INPUT 加 seq，服务端 ACK，客户端超时重发。
   - 或更简单：客户端对同一按下事件连续发 3 个冗余 INPUT（30Hz 内），容忍丢包。
   - STATE 包丢失无影响（下一帧覆盖），不需可靠。

7. **带宽优化**
   - 当前 STATE 广播全部实体。实体多后可做增量：只发变化的坦克/子弹，或只发玩家周围视野。
   - 坦克大战规模下当前全量已够用（几十字节/帧），优先级低。

### C. 游戏内容

8. **地图系统**
   - 随机生成：对称布局生成器，保证双方公平。
   - 可破坏墙：墙有 HP，子弹打一下掉一格，墙体变化要进 STATE。
   - 地图编辑：文本模板（当前 `_MAP_TEMPLATE`）可扩展为多关卡。

9. **扩展道具系统**
   - 当前已有缩小、三连发和一次性护盾；可继续加入加速、穿墙或稀有度。

10. **多种坦克 / 武器**
    - Tank 加 `type` 字段，不同类型血量/速度/弹速不同。
    - 子弹加 `kind`，普通弹/散弹/穿甲弹。

11. **游戏模式**
    - 死斗（当前）、夺旗、占点、生存（PVE）。
    - 模式由服务端 `room` 配置驱动，`game.py` 的 `step_world` 按模式分支。

### D. 交互与体验

12. **菜单与流程**
    - 客户端启动先显示菜单：连接/输入 IP/重开/退出。当前是直接进游戏。
    - 当前已支持结算后双方按开火确认重开；可继续增加独立菜单和重开消息。

13. **观战与录像**
    - 观战端：只收 STATE 不发 INPUT，服务端广播给额外地址。
    - 录像：客户端把每帧 STATE 落盘，回放时按 tick 重放。

14. **局数与赛制**
    - 当前为先到 5 分；可扩展多局制总比分和可配置目标分数。

### E. 多人与平台

15. **2 人以上**
    - `MAX_CLIENTS` 提到 4/6，出生点扩展到 4/6 个对称位置。
    - 分队：Tank 加 `team` 字段，友军不伤或单独配色。

16. **跨平台**
    - `input_win.py` 抽象成 `input.py` 接口，按平台 import 实现：
      - Linux/macOS：`termios` + `select` 读 `/dev/tty`，或 `curses` 的 `getch`。
      - 渲染层 `renderer.py` 用 ANSI 已跨平台，基本不用改。

## 四、优化点

### 性能

1. **服务端类化**
   - 权威世界已封装为 `World`；网络生命周期仍在较长的 `main()` 中，可进一步封装为 `GameServer`。

2. **服务端逻辑与收包解耦**
   - 当前 `step_world` 在主循环锁内调用，锁粒度可优化：输入入队加锁，世界推进无锁（单线程主循环独占）。减少接收线程阻塞。

3. **渲染双缓冲**
   - 当前差分渲染直接写 stdout，高频时可能撕裂。可先写全帧到字符串 buffer，一次性 flush。

### 健壮性

4. **掉线检测**
   - 服务端定时检测客户端最近 INPUT 时间，超时（如 5s）标记坦克离线、广播。
   - 客户端检测 STATE 中断（如 3s 无包），提示重连。

5. **ping 平滑**
   - 当前已有 PING/PONG 实测 RTT；可增加滑动平均和抖动指标。

6. **协议不兼容反馈**
   - 服务端已拒绝非 v3 JOIN；可增加明确的版本不兼容响应，避免客户端只表现为超时。

7. **可靠输入**
   - 当前已按 30Hz 发送持续移动意图；关键开火与重开确认仍可增加序号、ACK 或冗余发送。

### 架构

8. **服务端类化**
   - `server.py` 的 `main()` 函数过长，闭包变量多。重构为 `GameServer` 类，状态封装在实例属性，便于多房间扩展。

9. **确定性化（若未来转帧同步）**
   - 当前状态同步不需要确定性。若未来改帧同步以省带宽，需消除所有非确定性：随机数用种子、迭代顺序固定、禁用 `time` 参与逻辑。

### 可观测性

10. **调试视图**
    - 客户端加 F1 切换调试 overlay：显示 tick、预测位置 vs 服务器位置偏差、收包速率。
    - 服务端加 `--verbose` 输出每 tick 实体数、输入队列长度。

## 五、开发约定

- 改 `game.py` 后跑 `python test_logic.py`。
- 改 `input_win.py` 后跑 `python test_input.py` 确认按键识别。
- 改 `protocol.py` 后两端都要更新，且跑协议往返测试。
- 不引入第三方依赖（当前纯标准库 + ctypes），保持原型轻量。
- 坐标/状态用整数，避免浮点。
- 新增游戏规则优先改 `game.py` 的 `step_world`，保持逻辑层纯净。
