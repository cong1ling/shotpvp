# ShotPVP 竞技乱斗玩法升级设计

## 目标

把无限重生射击原型升级为有明确胜负、弹道博弈、地图变化和随机能力的双人竞技乱斗。保持服务端权威、标准库实现、整数格坐标和纯逻辑层可测试。

## 对局规则

- 地图扩大为 64×30 的对称竞技场。
- 双方经过 3 秒倒计时后开始，先获得 5 分者获胜。
- 游戏结束后停止移动、射击和伤害；双方按开火键即可请求重开。两人都请求后开始新一局。
- 击杀对手得 1 分；被自己的反弹子弹击杀时，对手获得 1 分。
- 死亡后按既有短倒计时重生，出生区域被占用时延迟重生。

## 坦克与碰撞

- 普通坦克主体占 3×3 格，坐标表示主体中心。
- 炮管伸出主体 1 格，只参与显示和确定枪口，不参与移动、墙体或坦克碰撞。
- 八个方向分别绘制明确的履带、炮塔和炮管；WASD 组合斜向，方向键保持四向。
- 缩小状态占 1×1 格；效果结束时若 3×3 空间不足，则保持缩小，直到空间可恢复。
- 子弹从炮管外侧的枪口格生成，不会在生成时命中发射者；此后不再免疫发射者。

## 墙体与反弹子弹

- 地图格类型：空地、砖墙、钢墙。
- 子弹碰到边界、砖墙或钢墙都反弹；砖墙同时立刻被破坏，钢墙和边界不受损。
- 正面撞击时翻转对应轴向；角落同时阻挡两个轴时翻转两个轴。
- 每次碰撞最多计为一次反弹。达到 5 次反弹后，子弹在完成该次墙体效果后消失。
- 子弹可击杀所有坦克，包括所有者。护盾优先抵消命中并消耗一层。
- 子弹之间不碰撞，避免规则过度复杂。

## 道具

- 服务端每 8 秒尝试生成一个道具，场上最多 2 个；只生成在能容纳普通 3×3 坦克的空地，避开坦克、子弹和其他道具。
- 道具种类等概率随机：
  - 缩小：持续 10 秒，主体变成 1×1；再次拾取刷新持续时间。
- 三连发：储存一次强化射击；下一次开火沿当前方向发射三条相邻的平行弹道。
  - 护盾：获得一层，最多一层，抵消下一次子弹伤害。
- 坦克主体覆盖道具格即拾取，由服务端判定。

## 状态与协议

完整 STATE 快照增加：对局阶段、阶段剩余 tick、胜者、可变地图、道具，以及坦克的缩小剩余 tick、护盾和三连发状态。Bullet 增加反弹次数。

客户端仍只预测本地移动；墙体破坏、拾取、弹射、伤害、计分和胜负全部以服务端快照为准。地图规模和实体数量下，全量快照仍足够轻量。

## 模块边界

- `game.py`：地图、3×3 占位、反弹、道具、阶段和胜负的纯规则。
- `protocol.py`：扩展后的 STATE 二进制编解码。
- `server.py`：持有权威世界、随机数和重开请求，推进并广播。
- `client.py`：输入请求、本地占位预测、快照重建和阶段提示。
- `renderer.py`：多格坦克、炮管、墙体、道具与结算 HUD。

## 错误与边界处理

- 非法或截断 STATE 返回 `None`。
- 子弹处于角落时只进行一次碰撞结算，避免同 tick 重复计数。
- 缩小结束、重生和道具生成都必须验证完整占位空间。
- 地图和计数继续受单字节字段上限约束；64×30 地图通过固定 2-bit/格编码传输。

## 验证

- 纯逻辑测试覆盖 3×3 移动、炮口生成、边界/砖墙/钢墙/角落反弹、自杀、反弹耗尽、护盾、三连发、缩小恢复、道具生成与拾取、先到 5 分及重开。
- 协议测试覆盖扩展快照往返与截断包。
- 编译检查覆盖全部 Python 文件。
- 交互手测覆盖 Unicode 多格绘制、长按移动、HUD、双方重开与终端尺寸。
